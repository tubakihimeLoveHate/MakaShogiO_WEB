using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace common
{
    public static class CommonCalcuration
    {

        //public static void CalculateFieldPosition(Vector3 basePosition, Vector3 offsetPosition, ref FieldCell[,] fieldCell)
        //{
        //    for (int v = 0; v < 9; v++)
        //    {
        //        for (int h = 0; h < 9; h++)
        //        {
        //            Debug.Log("kokomade");
        //            fieldCell[v, h].cellPosition = new Vector3(basePosition.x + offsetPosition.x * h, basePosition.y - offsetPosition.y * v, 0);
        //        }
        //    }
        //}

    }
}
